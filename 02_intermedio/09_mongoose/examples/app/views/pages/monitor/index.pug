extends ../../layouts/main-layout.pug
block content
  main
    h2 Date
    | #{ date }
    h2 Session id:
    | #{ sessionID }
    h2 Headers:
    ul
      each val, key in headers
        li #{ key }: #{ val }
